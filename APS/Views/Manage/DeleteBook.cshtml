@model APS.Models.ManageViewModels.DeleteBookViewModel;
@{
    ViewData["Title"] = "Deletar Livro";
}
<h2>@ViewData["Title"]</h2>
<h4>Não será possível recuperar os dados apagados</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post" asp-action="DeleteBook">
            @*<div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Password" class="control-label">Senha</label>
                <input asp-for="Password" class="form-control" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>*@
            <input type="hidden" asp-for="BookID" value="@ViewBag.book"/>
            <button type="submit" class="btn btn-default">Deletar</button>
        </form>
    </div>
</div>